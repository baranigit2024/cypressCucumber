{
  "devDependencies": {
    "cypress": "^10.1.0"
  },

  
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^18.0.4",
    "@cypress/browserify-preprocessor": "^3.0.2"
  },

  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/e2e/step-defination/Test/*.js",
    "filterSpecs": true,
    "omitFiltered": true
   }
}
